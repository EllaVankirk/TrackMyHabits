@model List<TrackMyHabit.Models.AllDates>

<h1>All Dates</h1>

@if (Model.Count == 0)
{
    <p> No dates yet!</p>
}
else
{
    <table class="table">
        <tr>
            <th>ID</th>
            <th>Dates</th>
        </tr>
        @foreach (AllDates dates in Model)
        {

        }
    </table>
}